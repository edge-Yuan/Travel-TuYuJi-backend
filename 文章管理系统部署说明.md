# 文章管理系统部署说明

## 概述

本文档说明如何部署和运行文章管理系统，包括后端API和前端页面的完整功能。

## 系统架构

- **后端服务**: travel-admin (端口: 8081)
- **前端页面**: Vue.js 单页面应用
- **数据库**: MySQL

## 部署步骤

### 1. 数据库准备

执行数据库脚本创建表结构：

```bash
mysql -u root -p your_database < database/create_article_management_tables.sql
```

### 2. 后端服务启动

#### 2.1 启动 travel-admin 服务

```bash
cd travel-admin
mvn spring-boot:run
```

或者使用IDE直接运行 `TravelAdminApplication.java`

#### 2.2 验证后端服务

访问: http://localhost:8081/test/health

应该返回：
```json
{
  "code": 1,
  "msg": "操作成功",
  "data": {
    "status": "UP",
    "timestamp": "2024-12-19T10:30:00",
    "message": "旅游管理系统后台管理模块运行正常"
  }
}
```

### 3. 前端页面部署

#### 3.1 文件准备

将以下文件放置到你的前端项目中：
- `test.vue` - 文章管理主页面
- `article-api-service.js` - API服务文件

#### 3.2 安装依赖

确保项目中已安装以下依赖：
```bash
npm install axios
```

#### 3.3 配置API地址

在 `article-api-service.js` 中确认API地址：
```javascript
const api = axios.create({
  baseURL: 'http://localhost:8081/travel-admin', // 确认端口正确
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})
```

## API接口说明

### 文章管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/article/page` | GET | 分页查询文章列表 |
| `/article/{id}` | GET | 获取文章详情 |
| `/article/create` | POST | 创建文章 |
| `/article/update` | PUT | 更新文章 |
| `/article/{id}` | DELETE | 删除文章 |
| `/article/batch` | DELETE | 批量删除文章 |
| `/article/batch/status` | PUT | 批量更新文章状态 |
| `/article/{id}/publish` | PUT | 发布文章 |
| `/article/{id}/unpublish` | PUT | 下架文章 |
| `/article/{id}/copy` | POST | 复制文章 |
| `/article/statistics` | GET | 获取文章统计数据 |

### 文章模板接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/article-template/list` | GET | 获取所有启用的模板列表 |
| `/article-template/category/{category}` | GET | 根据分类获取模板列表 |
| `/article-template/{id}` | GET | 获取模板详情 |
| `/article-template/create` | POST | 创建模板 |
| `/article-template/update` | PUT | 更新模板 |
| `/article-template/{id}` | DELETE | 删除模板 |

## 功能特性

### 文章管理功能

1. **文章列表管理**
   - 分页查询
   - 多条件筛选（状态、分类、地区、关键词）
   - 批量操作（发布、下架、删除）
   - 文章统计

2. **文章编辑功能**
   - 富文本编辑器（支持Markdown）
   - 封面图片上传
   - 标签管理
   - SEO设置
   - 关联套餐

3. **文章模板功能**
   - 预设模板
   - 模板分类
   - 模板应用

4. **文章操作功能**
   - 发布/下架
   - 复制文章
   - 预览功能
   - 统计数据

### 数据统计功能

- 总文章数
- 已发布文章数
- 总阅读量
- 总点赞数

## 数据库表结构

### 主要表

1. **article** - 文章主表
2. **article_package** - 文章套餐关联表
3. **article_template** - 文章模板表
4. **article_statistics** - 文章统计数据表

## 注意事项

1. **跨域问题**: 如果前端和后端不在同一域名，需要配置CORS
2. **文件上传**: 封面图片上传功能需要配置文件存储路径
3. **权限控制**: 当前版本未实现用户权限控制，生产环境需要添加
4. **数据验证**: 前端和后端都有数据验证，确保数据完整性

## 故障排除

### 常见问题

1. **API请求失败**
   - 检查后端服务是否启动（端口8082）
   - 确认API地址配置正确（http://localhost:8082/travel-admin）
   - 查看浏览器控制台错误信息
   - 检查CORS配置

2. **数据库连接失败**
   - 检查数据库服务是否启动
   - 确认数据库连接配置
   - 验证表结构是否正确创建
   - 执行数据库脚本：`database/create_article_management_tables_fixed.sql`

3. **前端页面显示异常**
   - 检查Vue组件是否正确引入
   - 确认Element UI组件库已安装
   - 查看浏览器控制台错误信息
   - 确认API服务文件路径正确

4. **编译错误**
   - 检查Java版本（需要Java 21）
   - 确认Maven依赖完整
   - 检查Swagger和Validation依赖

### 已验证功能

✅ 后端服务启动正常（端口8082）
✅ 数据库连接正常
✅ 文章API工作正常
✅ 模板API工作正常
✅ 统计数据API工作正常
✅ 分页查询功能正常
✅ JSON数据处理正常

## 扩展功能

可以考虑添加的功能：

1. 用户权限管理
2. 文章评论系统
3. 文章分类管理
4. 文件上传管理
5. 数据导入导出
6. 文章审核流程
7. 搜索引擎优化
8. 移动端适配

## 技术支持

如有问题，请检查：
1. 日志文件
2. 浏览器控制台
3. 网络请求状态
4. 数据库连接状态
