# 统一评价系统使用文档

## 📋 概述

统一评价系统整合了套餐评价和订单管理评价的功能，提供一致的数据结构和API接口，解决了之前数据不一致的问题。

## 🏗️ 系统架构

### 1. 数据模型
- **UnifiedEvaluationVO**: 统一评价展示数据模型
- **UnifiedEvaluationDTO**: 统一评价请求数据模型
- **TourEvaluation**: 数据库实体模型

### 2. 服务层
- **IUnifiedEvaluationService**: 统一评价服务接口
- **UnifiedEvaluationServiceImpl**: 统一评价服务实现

### 3. 控制器层
- **UnifiedEvaluationController**: 统一评价控制器

## 🔧 核心功能

### 1. 评价管理
- ✅ 添加评价
- ✅ 获取产品评价列表
- ✅ 获取订单评价列表
- ✅ 获取用户评价列表
- ✅ 获取用户对产品的评价
- ✅ 获取产品评价汇总信息
- ✅ 回复评价
- ✅ 删除评价
- ✅ 获取所有评价（管理员用）

### 2. 数据统一
- ✅ 统一数据结构
- ✅ 统一API接口
- ✅ 统一业务逻辑
- ✅ 统一前端调用

## 📡 API接口

### 基础路径
```
/travel-portal/evaluation
```

### 接口列表

#### 1. 添加评价
```http
POST /travel-portal/evaluation/add
```

**请求体：**
```json
{
  "orderId": 12345,
  "productId": 40003,
  "userId": 10003,
  "overallScore": 5,
  "serviceScore": 4,
  "environmentScore": 5,
  "costEffScore": 4,
  "content": "行程安排合理，酒店环境很好，非常满意的一次旅行",
  "imgUrls": "/images/eval/eval1.jpg,/images/eval/eval2.jpg",
  "evalType": 1,
  "targetId": 40003,
  "userType": "情侣/朋友",
  "source": "来自WWW站"
}
```

#### 2. 获取产品评价列表
```http
GET /travel-portal/evaluation/product/{productId}?page=1&size=10
```

#### 3. 获取订单评价列表
```http
GET /travel-portal/evaluation/order/{orderId}
```

#### 4. 获取用户评价列表
```http
GET /travel-portal/evaluation/user/{userId}?page=1&size=10
```

#### 5. 获取用户对产品的评价
```http
GET /travel-portal/evaluation/user/{userId}/product/{productId}
```

#### 6. 获取产品评价汇总
```http
GET /travel-portal/evaluation/product/{productId}/summary
```

#### 7. 回复评价
```http
POST /travel-portal/evaluation/reply/{evalId}?replyContent=感谢您的评价
```

#### 8. 删除评价
```http
DELETE /travel-portal/evaluation/{evalId}
```

#### 9. 获取所有评价（管理员）
```http
GET /travel-portal/evaluation/list?page=1&size=10&evalType=1
```

## 🗄️ 数据库更新

### 1. 执行数据库更新脚本
```sql
-- 执行以下SQL文件
source database/update_evaluation_table_unified.sql;
```

### 2. 新增字段说明
- `product_id`: 关联产品ID
- `user_id`: 评价用户ID
- `user_name`: 用户姓名
- `user_avatar`: 用户头像
- `user_type`: 用户类型
- `useful_count`: 有用数
- `source`: 评价来源

## 💻 前端使用

### 1. 引入API类
```javascript
import { UnifiedEvaluationAPI } from './unified-evaluation-api.js';
```

### 2. 基本使用示例
```javascript
// 添加评价
const evaluationData = {
  orderId: 12345,
  productId: 40003,
  userId: 10003,
  overallScore: 5,
  content: '非常满意的一次旅行',
  imgUrls: '/images/eval/eval1.jpg'
};

const response = await UnifiedEvaluationAPI.addEvaluation(evaluationData);
```

### 3. 获取评价列表
```javascript
// 获取产品评价列表
const evaluations = await UnifiedEvaluationAPI.getProductEvaluations(40003, 1, 10);

// 获取订单评价列表
const orderEvaluations = await UnifiedEvaluationAPI.getOrderEvaluations(12345);

// 获取用户评价列表
const userEvaluations = await UnifiedEvaluationAPI.getUserEvaluations(10003, 1, 10);
```

## 🔄 迁移指南

### 1. 从旧接口迁移
```javascript
// 旧接口调用
// POST /travel-portal/productEvaluation/add
// POST /travel-portal/tourEvaluation/create

// 新统一接口调用
// POST /travel-portal/evaluation/add
```

### 2. 数据格式统一
```javascript
// 旧格式（套餐评价）
{
  "evalId": 80004,
  "orderId": 60026,
  "productId": 40002,
  "userId": 10003,
  "userName": "张***",
  "userAvatar": "https://example.com/user-avatar.png",
  "userType": "情侣/朋友",
  "overallScore": 5,
  "serviceScore": 5,
  "environmentScore": 4,
  "costEffScore": 4,
  "content": "111111111111111",
  "imgUrls": "",
  "evalTime": "2025-10-04T21:29:28",
  "replyContent": null,
  "replyTime": null,
  "usefulCount": 12,
  "source": "来自WWW站"
}

// 新统一格式
{
  "evalId": 80004,
  "orderId": 60026,
  "productId": 40002,
  "userId": 10003,
  "userName": "张***",
  "userAvatar": "https://example.com/user-avatar.png",
  "userType": "情侣/朋友",
  "overallScore": 5,
  "serviceScore": 5,
  "environmentScore": 4,
  "costEffScore": 4,
  "content": "111111111111111",
  "imgUrls": "",
  "evalTime": "2025-10-04T21:29:28",
  "createTime": "2025-10-04T21:29:28",
  "replyContent": null,
  "replyTime": null,
  "usefulCount": 12,
  "source": "来自WWW站",
  "evalType": 1,
  "targetId": 40002,
  "productName": "产品名称",
  "productImage": "产品图片",
  "orderNumber": "订单号",
  "orderStatus": "订单状态"
}
```

## ✅ 优势

### 1. 数据一致性
- 统一的数据结构
- 统一的数据来源
- 统一的数据处理逻辑

### 2. 接口统一
- 统一的API路径
- 统一的请求格式
- 统一的响应格式

### 3. 功能完整
- 支持所有评价功能
- 支持多种查询方式
- 支持管理员功能

### 4. 易于维护
- 单一数据源
- 统一的业务逻辑
- 清晰的代码结构

## 🚀 部署步骤

### 1. 后端部署
1. 将新的Java文件部署到项目中
2. 重启Spring Boot应用
3. 验证接口是否正常工作

### 2. 数据库更新
1. 执行数据库更新脚本
2. 验证数据是否正确更新
3. 检查索引是否创建成功

### 3. 前端更新
1. 更新API调用路径
2. 更新数据格式处理
3. 测试所有功能

## 🔍 测试验证

### 1. 接口测试
```bash
# 测试添加评价
curl -X POST http://localhost:8086/travelManagementSystem/travel-portal/evaluation/add \
  -H "Content-Type: application/json" \
  -d '{"orderId":12345,"productId":40003,"userId":10003,"overallScore":5,"content":"测试评价"}'

# 测试获取产品评价列表
curl http://localhost:8086/travelManagementSystem/travel-portal/evaluation/product/40003?page=1&size=10
```

### 2. 数据验证
```sql
-- 检查数据是否正确更新
SELECT eval_id, order_id, product_id, user_id, user_name, user_avatar, user_type, useful_count, source 
FROM tour_evaluation 
LIMIT 5;
```

## 📞 技术支持

如有问题，请联系开发团队或查看相关文档。

---

**统一评价系统** - 让评价管理更简单、更统一！
